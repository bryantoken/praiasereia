<nav class="navbar navbar-expand-lg p-0 pt-0 pb-0 sticky-top onTop">

    <div class="container-fluid row m-0">
        <!--########################################################-->
        <div class="navbar-brand d-flex align-items-center col-10 col-md-3 m-0">
            <div class="col-12">
                <a class="navbar-brand" href="/">
                    <%= image_tag("logo_branca_color.png", class: "logo-img img-fluid" , alt: "Logo da empresa" ) %>
                </a>
            </div>
            <div class="col-2 d-flex justify-content-center">
                <button class="btn bg-transparent text-light p-0 d-flex d-md-none border-0" type="button"
                    data-bs-toggle="offcanvas" data-bs-target="#offcanvas" aria-controls="offcanvas">
                    <ion-icon name="menu-outline" style="font-size: 2em;"></ion-icon>
                </button>
            </div>
        </div>
        <!--########################################################-->
        <div class="col-md-4 d-md-flex d-none d-flex align-items-center fluid">
            <% if current_page?(root_path) %>
                <div></div>
                <% else %>
                    <%= render 'search/form' %>
                        <% end %>
        </div>
        <!--########################################################-->
        <div class="d-none d-md-flex col-md-5 justify-content-center fluid text-responsive">
            <div class="col-md-12">
                <ul class="navbar-nav justify-content-center flex-row">
                    <li class="nav-item d-inline p-1">
                        <%= link_to "Moradores" , moradors_path, class:"roboto-thin nav-link-custom nav-link
                            text-responsive"%>
                    </li>
                    <li class="nav-item d-inline p-1">
                        <%= link_to "Solicitações" , welcome_index_path, class:"roboto-thin nav-link-custom nav-link
                            disabled"%>
                            <!-- Em construção -->
                    </li>
                    <li class="nav-item d-inline p-1">
                        <% if user_signed_in? %>
                            <div class="text-light rounded-2 p-1">
                                   <div class="dropdown">
  <button class="btn text-light btn-transparent dropdown-toggle p-0 d-flex align-items-center justify-content-center" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
    <%= current_user.email %>
  </button>
  <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="dropdownMenuButton">
    <li>
      <%= link_to destroy_user_session_path, method: :delete, class: "dropdown-item formatar text-danger" do %>
        <span>Sair</span>
        <ion-icon name="log-out-outline"></ion-icon>
      <% end %>
    </li>
  </ul>
</div>

                            </div>
                            <% else %>
                                <%= button_to user_session_path, method: :get,
                                    class: "btn btn-primary d-flex flex-row align-items-center" do %>
                                    Entrar
                                    <% end %>
                                        <% end %>

                    </li>

                </ul>

            </div>

        </div>

    </div>

</nav>